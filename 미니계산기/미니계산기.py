#coding=utf-8runcheck = 0   #이전 계산을 이용할건지 리셋하고 새로운 계산을 할건지 판별하는 전역변수num_a = int()num_b = int()#작동 확인def fnum():       #첫번째 숫자를 입력받아 출력하며 입력값을 돌려줌	num = int(input("첫번째 숫자를 입력하세요"))	print ("첫번째 숫자는",num,"입니다")	return num#작동확인def snum():        #두번째 숫자를 입력받아 출력하며 입력값을 돌려줌	num = int(input("두번째 숫자를 입력하세요"))	print ("두번째 숫자는",num,"입니다")	return num#작동확인def resetcheck():   #runcheck변수를 통해 이전값을 유지하고 계산을 할지 새로운 계산을 할지 판단하는 함수	a=input("두번째 숫자를 입력하세요 만약 새로운 계산을 원하면 reset을 입력하세요")	global num_b	global runcheck	if a == "reset":		runcheck = 0	else:		num_b = int(a)	#작동확인def ansis(a):	#정답을 출력하고 runcheck값을 1로 변경,또한 num_a에 계산된값을 전달	global num_a	global runcheck	runcheck = 1	num_a = int(ans)	print(a)	def askcount(a,b):	ask = input("+,-,*,/ 중 원하는 연산을 입력하세요")	if ask == "+":		ans = a+b		ansis(ans)	elif ask == "-":        ans = a-b        ansis(ans)    elif ask == "*":        ans == a*b        ansis(ans)    elif ask == "/":        ans == a/b        ansis(ans)	else :		print("잘못 입력하셨습니다.")		askcount(a,b)    while 1:    if runcheck==0:        num_a = fnum()        num_b = snum()        askcount(num_a,num_b)    elif runcheck==1:        resetcheck()        if runcheck == 0:            pass        elif runcheck == 1:            askcount(num_a,num_b)            	     